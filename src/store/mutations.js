export const setSearchedMeals = (state, meals) => {
    state.searchedMeals = meals;
}

export const setSearchedMealsByLetter = (state, meals) => {
    state.searchedMealsByLetter = meals;
}

export const setSearchedMealsByIngredient = (state, meals) => {
    state.searchedMealsByIngredient = meals;
}
